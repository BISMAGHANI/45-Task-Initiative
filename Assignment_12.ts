"use strict";
let names = ["Hudebiya", "Haniya", "Humna", "Maryam"];
const message = `Welcome to Typescript Class;`;
for (let i = 0; i < names.length; i++) {
    console.log(message + names[i]);
}
